<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/choxx"
    tools:context=".DebitCard" >

    <ListView
        android:id="@+id/lvDebitCards"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/addNewNb" >
    </ListView>

    <ImageButton
        android:id="@+id/addNewNb"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/lvDebitCards"
        android:layout_alignParentTop="true"
        android:layout_marginTop="186dp"
        android:src="@android:drawable/ic_input_add" />

    <ImageView
        android:id="@+id/imageView1"
        android:layout_width="320dp"
        android:layout_height="180dp"
        android:layout_alignParentLeft="true"
        android:layout_alignRight="@+id/addNewNb"
        android:src="@drawable/debit_card" />

</RelativeLayout>